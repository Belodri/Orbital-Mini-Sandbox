{
    "name": "bridge",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "build:js": "vite build",
        "build:types": "tsc -p tsconfig.declarations.json",
        "build": "npm run build:js && npm run build:types"
    },
    "devDependencies": {
        "typescript": "^5.8.3",
        "vite": "^7.1.6"
    }
}
